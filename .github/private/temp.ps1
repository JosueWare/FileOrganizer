New-Item (Join-Path -Path "$folderMyDocuments" "FileOrganizer") -ItemType Directory

New-Item (Join-Path -Path "$folderMyPictures" "FileOrganizer") -ItemType Directory

New-Item (Join-Path -Path "$folderMyVideos" "FileOrganizer") -ItemType Directory